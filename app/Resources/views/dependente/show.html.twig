{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="page-header">Dependente #{{ dependente.id }}</h2>

    <ul class="nav nav-pills" style="margin-bottom:15px">
        <li>
        <a class="btn btn-primary" href="{{ path('dependente_index') }}" role="button" aria-label="Lista">
            <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> Lista
        </a>
        </li>
        <li>
        <a class="btn btn-primary" href="{{ path('dependente_edit', { 'id': dependente.id }) }}" role="button" aria-label="Editar">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
        </a>
        </li>
        <li>
        {{ form_start(delete_form) }}
            <button class="btn btn-danger" type="submit" ><span class="glyphicon glyphicon-remove" aria-hidden="true"></span> Apagar</button>
        {{ form_end(delete_form) }}
        </li>
    </ul>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th class="col-md-3">Nome</th>
                <td>{{ dependente.nome }}</td>
            </tr>
            <tr>
                <th>Data de Nascimento</th>
                <td>{% if dependente.dataNascimento %}{{ dependente.dataNascimento|date('d/m/Y') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Funcionário</th>
                <td>{{ dependente.funcionario.nome }}</td>
            </tr>
            <tr>
                <th>Parentesco</th>
                <td>{{ dependente.parentesco }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
